[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Bot de inventário para Discord com comandos slash e persistência SQLite"
requires-python = ">=3.11"
dependencies = [
    "discord-py>=2.7.1",
]
